import Default from 'api/CKeyDefault'
import Methods from 'api/CKeyMethods'
import Formula from 'api/CKeyFormula'

const CKeyApi = {
  D: Default,
  M: Methods,
  F: Formula
}

export default CKeyApi
